---
description: Admin — maintenance (pull, changelog, doctor)
---

# Admin

Maintenance workflow.

Flow:
1. Pull latest ECC + 9Router
2. Doctor check (dependencies, config, health)
3. System info
4. Summary

Jalankan: `.\scripts\admin.ps1`
Atau: `.\scripts\admin.ps1 -Doctor` (check only)
